# 2-2-Exercises
2-2 Assignment

##Text01.java
Set a breakpoint at line 5 and had the first 3 print statements run on one single line, rather than 3 separate lines. This one shows that while whitespace isn't required, it is beneficial for readability.

##text02.java
An additional closing bracket was included in the code on line 9, which was closing out the main - line 4 was no longer being run. Removed the closing bracket, code now runs as was expected. This one shows how brackets set boundaries for methods.

##text03.java
Actually struggled with this for a bit, the instructions on the slide didn't really make much sense at first, and I kept running into problems no matter any method I tried. Ended up being because I was using drawlegs() instead of drawLegs(). Code now runs as expected. While the initial method was stated, it wasn't being ran because it hadn't been called. 
